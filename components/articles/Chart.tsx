'use client'

// Generic Chart component — data-as-props from MDX is unreliable.
// Use article-specific named components instead (e.g. MetaSegmentationCharts.tsx)
export default function Chart() {
  return null
}
